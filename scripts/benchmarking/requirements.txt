# test runner
pytest

# Progress bars
tqdm

# DataFrame-based evaluation (evaluation.py, evaluations/)
pandas

# HTTP client for llama-server calls (llama_server.py)
requests

# Configuration (config.py)
pydantic-settings

# GGUF metadata reader (metrics.py)
gguf

# TruthfulQA scoring: ROUGE-L + TF-IDF cosine similarity
rouge-score
scikit-learn

# HumanEval: pass@k code execution metric
evaluate
datasets
